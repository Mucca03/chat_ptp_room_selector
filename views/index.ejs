<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chat P2P - Universidad Distrital</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: linear-gradient(135deg, #1b2735, #090a0f);
        color: #fff;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        min-height: 100vh;
        margin: 0;
        padding: 20px;
      }

      h1 {
        margin-top: 10px;
        color: #00bfff;
      }

      #container {
        display: flex;
        flex-direction: column;
        align-items: stretch;
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(10px);
        border-radius: 15px;
        padding: 20px;
        width: 100%;
        max-width: 700px;
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.4);
      }

      #connection {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        justify-content: center;
        margin-bottom: 15px;
      }

      input[type="text"] {
        padding: 10px;
        border: none;
        border-radius: 8px;
        outline: none;
        width: 180px;
      }

      button {
        background-color: #00bfff;
        color: white;
        border: none;
        padding: 10px 15px;
        border-radius: 8px;
        cursor: pointer;
        font-weight: bold;
        transition: background 0.3s ease;
      }

      button:hover {
        background-color: #0099cc;
      }

      #messagesent {
        background: rgba(255, 255, 255, 0.1);
        border-radius: 10px;
        padding: 10px;
        min-height: 250px;
        max-height: 350px;
        overflow-y: auto;
        margin-bottom: 10px;
      }

      #yourname, #remotename {
        font-size: 14px;
        color: #ccc;
        text-align: center;
      }

      #footer {
        text-align: center;
        margin-top: 15px;
        font-size: 13px;
        color: #999;
      }
    </style>
  </head>

  <body>
    <h1>Chat P2P - Test</h1>
    <p>Mauricio Alejandro Bernal Rodríguez - 20202578092</p>
    <p>
    
    <div id="container">
      <div id="connection">
        <input type="text" id="nombresala" placeholder="Nombre de la sala" />
        <button id="connectbutton">Conectar</button>
        <input type="text" id="nombreusser" placeholder="Tu nombre" />
        <input type="text" id="messagearea" placeholder="Escribe un mensaje" />
        <button id="sendmessage">Enviar</button>
      </div>

      <p id="yourname"></p>
      <p id="remotename"></p>

      <div id="messagesent"></div>
    </div>

    <div id="footer">
      Proyecto P2P Chat con WebRTC y Socket.IO
    </div>

    <!-- Dependencias -->
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://webrtc.github.io/adapter/adapter-latest.js"></script>

    <!-- Scripts principales -->
    <script src="js/config.js"></script>
    <script src="js/main.js"></script>
  </body>
</html>
